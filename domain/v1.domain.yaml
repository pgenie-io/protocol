
Request:
  sum:
    process:
      product:
        space: Name
        name: Name
        migrations: BVec (Path, Text)
        queries: BVec (Path, Text)
        artifacts: BVec Name

Response:
  sum:
    failed: Text
    generated: BVec (Path, Text)
