
Request:
  sum:
    process:
      product:
        space: Name
        name: Name
        migrations: BVec (Path, Text)
        queries: BVec (Path, Text)
        artifacts: BVec Artifact

Response:
  sum:
    failed: Text
    generated: BVec (Path, Text)

Artifact:
  enum:
    - haskellHasql
    - javaJdbc
