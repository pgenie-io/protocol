
Request:
  sum:
    process:
      product:
        org: Name
        name: Name
        migrations: List (Path, Text)
        queries: List (Path, Text)

Response:
  sum:
    failed: Text
    generated: List (Path, Text)
